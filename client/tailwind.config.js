/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
    },
    // trying to make the Poppin font work
    fontFamily: {
      'Poppins': ['nunito', 'sans-serif']
    },
  },
  plugins: [
  ],
}
